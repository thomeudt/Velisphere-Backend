ORDER BY (SORT)
 RECEIVE FROM ALL PARTITIONS
  SEND PARTITION RESULTS TO COORDINATOR
   NESTLOOP INDEX JOIN
   inline (INDEX SCAN of "CHECK" using "CHPR" (unique-scan covering))
    SEQUENTIAL SCAN of "PROPERTY"

