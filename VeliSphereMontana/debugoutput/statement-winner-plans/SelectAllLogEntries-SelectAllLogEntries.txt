SQL: SELECT * FROM LOGQUEUE ORDER BY IDENTIFIER;
COST: 5040.0
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 ORDER BY (SORT)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    INDEX SCAN of "LOGQUEUE" using "SYS_IDX_SYS_PK_10019_10020" (for sort order only)

