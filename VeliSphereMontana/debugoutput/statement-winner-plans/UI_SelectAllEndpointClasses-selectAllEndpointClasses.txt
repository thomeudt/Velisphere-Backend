SQL: SELECT * FROM ENDPOINTCLASS ORDER BY ENDPOINTCLASSNAME;
COST: 5000000.0
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 ORDER BY (SORT)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    SEQUENTIAL SCAN of "ENDPOINTCLASS"

