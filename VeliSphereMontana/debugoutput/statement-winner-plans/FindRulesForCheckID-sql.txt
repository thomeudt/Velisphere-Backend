SQL: SELECT RULEID FROM RULE WHERE CHECKID = ?;
COST: 3972.0
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 RECEIVE FROM ALL PARTITIONS
  SEND PARTITION RESULTS TO COORDINATOR
   INDEX SCAN of "RULE" using "RCHECK" (unique-scan covering)

