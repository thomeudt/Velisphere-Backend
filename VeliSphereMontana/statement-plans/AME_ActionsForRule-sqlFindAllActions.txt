SQL: SELECT ACTIONID FROM RULE_ACTION_LINK WHERE RULEID = ? ORDER BY ACTIONID;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 ORDER BY (SORT)
  INDEX SCAN of "RULE_ACTION_LINK" using its primary key index
   uniquely match (RULEID = ?0)

