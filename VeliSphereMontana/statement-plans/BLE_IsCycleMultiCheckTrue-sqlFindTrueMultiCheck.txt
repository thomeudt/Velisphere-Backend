SQL: SELECT MULTICHECKID FROM MULTICHECK WHERE MULTICHECKID = ? AND STATE = ? ORDER BY MULTICHECKID;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 ORDER BY (SORT)
  INDEX SCAN of "MULTICHECK" using "MCST"
   scan matches for (STATE = ?1), filter by (MULTICHECKID = ?0)

