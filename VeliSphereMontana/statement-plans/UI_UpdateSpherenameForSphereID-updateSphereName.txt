SQL: UPDATE SPHERE SET SPHERENAME = ? WHERE SPHEREID = ?;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 AGGREGATION ops: SUM(VOLT_TEMP_TABLE.modified_tuples)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    UPDATE
     INDEX SCAN of "SPHERE" using its primary key index
      uniquely match (SPHEREID = ?1), filter by (SPHEREID = ?1)

