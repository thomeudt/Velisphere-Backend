SQL: DELETE FROM MESSAGE_EXPRESSION_LINK WHERE (LINKID = ?);
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 LIMIT 1
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    DELETEMESSAGE_EXPRESSION_LINK
     INDEX SCAN of "MESSAGE_EXPRESSION_LINK" using its primary key index
      uniquely match (LINKID = ?0), filter by (LINKID = ?0)

