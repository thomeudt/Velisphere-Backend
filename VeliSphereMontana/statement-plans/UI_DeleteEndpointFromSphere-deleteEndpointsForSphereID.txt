SQL: DELETE FROM ENDPOINT_SPHERE_LINK WHERE ENDPOINTID = ? AND SPHEREID = ?;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 LIMIT 1
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    DELETEENDPOINT_SPHERE_LINK
     SEQUENTIAL SCAN of "ENDPOINT_SPHERE_LINK"
      filter by ((((SPHEREID = ?1) AND (ENDPOINTID = ?0)) AND (SPHEREID = ?1)) AND (ENDPOINTID = ?0))

