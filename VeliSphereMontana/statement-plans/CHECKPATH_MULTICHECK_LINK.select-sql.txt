SQL: SELECT * FROM CHECKPATH_MULTICHECK_LINK WHERE (LINKID = ?) AND (CHECKPATHID = ?);
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 INDEX SCAN of "CHECKPATH_MULTICHECK_LINK" using its primary key index
  uniquely match (LINKID = ?0) AND (CHECKPATHID = ?1)

