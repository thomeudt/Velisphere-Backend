SQL: SELECT CHECKID FROM CHECK WHERE ENDPOINTID = ? AND PROPERTYID = ? ORDER BY CHECKID;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 ORDER BY (SORT)
  INDEX SCAN of "CHECK" using "CHEP"
   scan matches for (ENDPOINTID = ?0), filter by (PROPERTYID = ?1)

