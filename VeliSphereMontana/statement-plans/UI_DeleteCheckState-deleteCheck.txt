SQL: DELETE FROM CHECKSTATE WHERE CHECKID = ?;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 Serial AGGREGATION ops: SUM(VOLT_TEMP_TABLE.modified_tuples)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    DELETE CHECKSTATE
     INDEX SCAN of "CHECKSTATE" using its primary key index
     range-scan on 1 of 2 cols from (CHECKID >= ?0) while (CHECKID = ?0), filter by (CHECKID = ?0)

