SQL: SELECT RULEID FROM RULE WHERE MULTICHECKID = ?;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 RECEIVE FROM ALL PARTITIONS
  SEND PARTITION RESULTS TO COORDINATOR
   INDEX SCAN of "RULE" using "RMCHECK"
    scan matches for (MULTICHECKID = ?0)

