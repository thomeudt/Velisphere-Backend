SQL: SELECT * FROM SPHERE_USER_LINK WHERE (LINKID = ?) AND (SPHEREID = ?);
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 INDEX SCAN of "SPHERE_USER_LINK" using its primary key index
  uniquely match (LINKID = ?0) AND (SPHEREID = ?1)

