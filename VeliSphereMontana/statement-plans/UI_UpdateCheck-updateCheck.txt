SQL: UPDATE CHECK SET NAME = ?, CHECKVALUE = ?, OPERATOR = ?, STATE = 0 WHERE CHECKID = ?;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 Serial AGGREGATION ops: SUM(VOLT_TEMP_TABLE.modified_tuples)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    UPDATE
     INDEX SCAN of "CHECK" using its primary key index
     range-scan on 1 of 3 cols from (CHECKID >= ?3) while (CHECKID = ?3), filter by (CHECKID = ?3)

