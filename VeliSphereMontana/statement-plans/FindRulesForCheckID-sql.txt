SQL: SELECT RULEID FROM RULE WHERE CHECKID IN ? ORDER BY RULEID;
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 ORDER BY (SORT)
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    NESTLOOP INDEX INNER JOIN
    inline INDEX SCAN of "RULE" using "RCHECK"
     scan matches for (CHECKID = materialized_temp_table.list_element)
     MATERIALIZED SCAN of SQL-IN-LIST (Sort INVALID)

