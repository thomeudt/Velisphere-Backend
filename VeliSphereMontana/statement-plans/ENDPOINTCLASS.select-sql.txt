SQL: SELECT * FROM ENDPOINTCLASS WHERE (ENDPOINTCLASSID = ?);
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 INDEX SCAN of "ENDPOINTCLASS" using its primary key index
  uniquely match (ENDPOINTCLASSID = ?0)

