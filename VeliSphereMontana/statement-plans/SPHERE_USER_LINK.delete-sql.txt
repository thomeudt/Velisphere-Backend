SQL: DELETE FROM SPHERE_USER_LINK WHERE (LINKID = ?) AND (SPHEREID = ?);
PLAN:
RETURN RESULTS TO STORED PROCEDURE
 LIMIT 1
  RECEIVE FROM ALL PARTITIONS
   SEND PARTITION RESULTS TO COORDINATOR
    DELETE SPHERE_USER_LINK
     INDEX SCAN of "SPHERE_USER_LINK" using its primary key index
      uniquely match (LINKID = ?0) AND (SPHEREID = ?1), filter by ((SPHEREID = ?1) AND (LINKID = ?0))

